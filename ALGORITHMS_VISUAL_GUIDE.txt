╔════════════════════════════════════════════════════════════════════════════╗
║                  DFS + A* ALGORITHMS - VISUAL GUIDE                        ║
╚════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. DFS (DEPTH-FIRST SEARCH) - CONCEPT EXTRACTION                           │
└─────────────────────────────────────────────────────────────────────────────┘

INPUT: Markdown Summary
──────────────────────

## Machine Learning
- Supervised Learning
  - Classification
  - Regression
- Unsupervised Learning
  - Clustering
  - Dimensionality Reduction

## Deep Learning
- Neural Networks
- CNNs
- RNNs


DFS EXTRACTION PROCESS:
─────────────────────

Summary Text
    ↓
Parse Headers (## = main, ### = sub)
    ↓
Parse Bullets (- = subconcept)
    ↓
Build Tree Structure
    ↓
DFS Traversal (top-to-bottom)

CONCEPT TREE:
────────────

        ML
       /  \
      SL   UL
     / \   / \
    C   R Cl  DR

DFS Order: ML → SL → C → R → UL → Cl → DR


OUTPUT: Hierarchical Topics
───────────────────────────

🧠 Summary Structure Analysis (DFS)
├─ Main Concepts: 2
├─ Sub-Topics: 4
├─ Complexity: Simple (2) | Moderate (0) | Complex (0)
└─ Topics in DFS Order:
   ├─ 🟢 Machine Learning (Easy) [2 subtopics]
   ├─ 🟡 Supervised Learning (Medium) [2 items]
   ├─ 🟡 Unsupervised Learning (Medium) [2 items]
   └─ 🔴 Deep Learning (Hard) [3 subtopics]


┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. A* (A-STAR) ALGORITHM - QUESTION RECOMMENDATION                         │
└─────────────────────────────────────────────────────────────────────────────┘

STUDENT STATE:
──────────────
Answered: Q1 ✅, Q2 ❌, Q3 ✅
Performance: 67% (2/3 correct)
Weak Topics: {loops, conditionals}
Strong Topics: {variables}


HEURISTIC CALCULATION:
──────────────────────

Candidate Questions:
├─ Q4 (loops, Difficulty 3)
│  h = 0
│  h -= 10  (weak topic) 
│  h -= 7   (optimal difficulty for 67%)
│  h -= 1   (variety bonus)
│  h = -18  ← HIGHEST PRIORITY ⭐
│
├─ Q5 (conditionals, Difficulty 2)
│  h = 0
│  h -= 10  (weak topic)
│  h -= 5   (student doing okay, encourage easier)
│  h = -15  ← HIGH PRIORITY
│
├─ Q6 (variables, Difficulty 1)
│  h = 0
│  h += 5   (already mastered)
│  h += 2   (asked recently)
│  h = 7    ← LOW PRIORITY
│
└─ Q7 (functions, Difficulty 4)
   h = 0
   h += 8   (too hard for current level)
   h += 15  (missing prerequisites)
   h = 23   ← LOWEST PRIORITY ❌


A* PRIORITY QUEUE:
──────────────────

min-heap: [(-18, Q4), (-15, Q5), (7, Q6), (23, Q7)]
                ↓
pop minimum: (-18, Q4)
                ↓
RECOMMEND: Q4 (loops question)


OUTPUT: Next Question
──────────────────────

User clicks: "Next →"
    ↓
A* Algorithm runs
    ↓
Recommends: Q4 (loops)
    ↓
Reason: Weak topic + optimal difficulty


PERFORMANCE ANALYSIS (After Quiz):
──────────────────────────────────

Results Dashboard:
┌──────────────────┬────────┬─────┐
│ Topic            │ Score  │ Ct  │
├──────────────────┼────────┼─────┤
│ loops            │ 33%    │ 1/3 │ ← Needs Review
│ conditionals     │ 50%    │ 1/2 │ ← Needs Review
│ variables        │ 100%   │ 3/3 │ ✅ Mastered
│ functions        │ 0%     │ 0/1 │ ← Skip for now
└──────────────────┴────────┴─────┘

Recommended Topics to Review (Ranked):
1. 🔴 loops (33% - critical)
2. 🟡 conditionals (50% - important)
3. 🟢 variables (100% - skip)
4. ⚫ functions (0% - not ready)


┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. INTEGRATION: HOW THEY WORK TOGETHER                                      │
└─────────────────────────────────────────────────────────────────────────────┘

COMPLETE QUIZ FLOW:
───────────────────

┌────────────────────────────────────────────────────────────┐
│ STEP 1: Upload PDF                                         │
│ ├─ Extract text                                            │
│ └─ Send to Gemini                                          │
└────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 2: Dashboard - Select Summary                         │
│ ├─ Load summary from database                              │
│ └─ Store in session state                                  │
└────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 3: Create Quiz Page - DFS ANALYSIS                    │
│ ├─ ConceptExtractor.build_quiz_topics()                    │
│ ├─ ConceptExtractor.analyze_concept_relationships()        │
│ └─ Display in "Summary Structure Analysis"                 │
└────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 4: Generate Quiz                                      │
│ ├─ Call Gemini API                                         │
│ ├─ Get JSON questions with metadata                        │
│ └─ Store in session_state["generated_quiz"]                │
└────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 5: Take Quiz - A* IN ACTION                           │
│ ├─ Display Q1                                              │
│ ├─ Student answers                                         │
│ ├─ Click "Next →"                                          │
│ │   ├─ Initialize QuizRecommender                          │
│ │   ├─ Build performance_history                           │
│ │   ├─ Call a_star_next_question()                         │
│ │   ├─ Get optimal Q2 index                                │
│ │   └─ Jump to Q2                                          │
│ └─ Repeat for all questions                                │
└────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 6: Performance Analysis - A* ANALYSIS                 │
│ ├─ recommender.get_performance_summary()                   │
│ ├─ Display topic-wise scores                               │
│ ├─ recommender.recommend_review_topics()                   │
│ └─ Show ranked review recommendations                      │
└────────────────────────────────────────────────────────────┘


ALGORITHM COMPLEXITY:
─────────────────────

DFS:
├─ Time: O(n) where n = number of lines in summary
├─ Space: O(c) where c = number of concepts
└─ Run time: < 1ms

A*:
├─ Time: O(n log n) where n = remaining questions
├─ Space: O(n) for priority queue
└─ Run time: 5-10ms per recommendation


HEURISTIC COMPONENTS:
─────────────────────

h(n) Score Breakdown:

     BASE: 0
      │
      ├─ WEAK TOPIC FACTOR
      │  └─ if topic in weak_topics: h -= 10
      │
      ├─ DIFFICULTY FACTOR
      │  ├─ if student_score < 0.5:
      │  │  ├─ if difficulty ≤ 2: h -= 5 (encourage easier)
      │  │  └─ if difficulty ≥ 4: h += 8 (discourage hard)
      │  └─ else (doing well):
      │     └─ if difficulty ≥ 4: h -= 6 (push harder)
      │
      ├─ PREREQUISITE FACTOR
      │  └─ if prerequisites_not_met: h += 15
      │
      └─ VARIETY FACTOR
         ├─ if asked_recently: h += 2
         └─ else: h -= 1

     FINAL h(n) = h_score (lower = higher priority)


┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. SAMPLE EXECUTION TRACE                                                   │
└─────────────────────────────────────────────────────────────────────────────┘

Initial State:
──────────────
Current Question: Q2
User Answer: Wrong (on "loops")
Performance: {Q0: 1.0, Q1: 0.0, Q2: 0.0}
Weak Topics: {loops}

A* Recommendation:
──────────────────

for q_idx in 3..10:
    if q_idx in answered_questions: continue
    
    g_score = q_idx - 2 = distance from current
    h_score = heuristic(q_idx, student_data)
    f_score = g_score + h_score
    
    heap.push((f_score, q_idx))

┌─────┬─────────────┬──────────┬──────────┬──────────┐
│ Q# │ Topic       │ g(n)     │ h(n)     │ f(n)     │
├─────┼─────────────┼──────────┼──────────┼──────────┤
│ 3  │ loops       │ 1        │ -15      │ -14      │ ← BEST
│ 4  │ loops       │ 2        │ -14      │ -12      │
│ 5  │ variables   │ 3        │ +5       │ +8       │
│ 6  │ conditionals│ 4        │ -8       │ -4       │
│ 7  │ arrays      │ 5        │ -10      │ -5       │
│ 8  │ functions   │ 6        │ +12      │ +18      │
│ 9  │ oop         │ 7        │ +15      │ +22      │
│ 10 │ classes     │ 8        │ +18      │ +26      │
└─────┴─────────────┴──────────┴──────────┴──────────┘

pop_min(heap) → (-14, Q3)

Next Question: Q3 (loops - weak area, optimal difficulty)


┌─────────────────────────────────────────────────────────────────────────────┐
│ 5. KEY TAKEAWAYS                                                            │
└─────────────────────────────────────────────────────────────────────────────┘

DFS: Extracts structure from unstructured text
     └─ Traverses tree hierarchically
     └─ Finds all relationships

A*: Finds optimal path with estimated cost
    └─ Balances actual_distance + estimated_value
    └─ More efficient than exhaustive search

Together: Smart learning system that:
          ✓ Understands content structure
          ✓ Adapts to student performance
          ✓ Recommends optimal progression
          ✓ Minimizes learning time

